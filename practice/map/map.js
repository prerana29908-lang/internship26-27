// const user = new Map();

// user.set("name", "Prerana");
// user.set("age", 20);

// console.log(user);

// A Map is a special object used to store key-value pairs.

// Unlike a normal object, a Map can use any data type as a 
// key (string, number, object, function, etc.).

// console.log(user.get("name"));
// console.log(user.has("age"));//true

// user.delete("age");

// console.log(user.size);

//Optional chaining
// const user = {
//     name: "Prerana",
//     address: {
//         city: "Mumbai"
//     }
// };

// console.log(user.address?.city);